spring:
 datasource:
  driver-class-name: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://127.0.0.1:3306/bbb
  username: root
  password: 123456
# security:
#  user:
#   name: user    #???
#   password: 123 #??
# jwt:
#   secret: "your_very_long_secret_key_here_with_at_least_32_bytes"  # 签名密钥，用于生成和验证JWT,密钥长度不小于32字节
#   expiration: 3600000         # 令牌有效期（单位：毫秒），如 3600000 表示 1 小时
mybatis-plus:
 configuration:
  map-underscore-to-camel-case: false
